public record Moneda(String result,
                     String base_code,
                     String target_Code,
                     double conversion_rate,
                     double conversion_result) {
}
